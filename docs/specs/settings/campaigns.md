# Campaigns settings page

Campaigns page allows the user:

- Campaigns CRUD
- Inside every campaign CRUD the response buttons
- In case responses are recorded, inside every campaign view the responses statistics and individual responses.

## Buttons order

There must be a way to re-order buttons in the campaign with a drag-and-drop and up/down arrow interface.
